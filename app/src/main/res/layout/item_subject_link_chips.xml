<?xml version="1.0" encoding="utf-8"?>
<!-- «Ссылки» with «Все», then chips added on bind: links, «Ещё N» and the add chip. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/design_spacing_compact"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/links_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:accessibilityHeading="true"
            android:paddingHorizontal="@dimen/design_spacing_related"
            android:text="@string/links_title"
            android:textAppearance="?attr/textAppearanceTitleSmall"
            tools:targetApi="p" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/all_links"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/links_all_description"
            android:minWidth="@dimen/design_touch_target"
            android:minHeight="@dimen/design_touch_target"
            android:paddingStart="@dimen/design_spacing_content"
            android:paddingEnd="@dimen/design_spacing_related"
            android:text="@string/links_all"
            app:icon="@drawable/ic_chevron_right"
            app:iconGravity="end"
            app:iconPadding="0dp" />

    </LinearLayout>

    <com.google.android.material.chip.ChipGroup
        android:id="@+id/chips"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:chipSpacingHorizontal="@dimen/design_spacing_compact"
        app:chipSpacingVertical="0dp" />

</LinearLayout>
